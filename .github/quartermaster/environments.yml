environments:
  - name: discourse_dev
    description: >-
      The Discourse development environment: Ruby, node, postgres and redis.
      Specs, rubocop, prettier, lint, migrations and the rails console.
    image: discourse/discourse_dev:release
    cmd: /sbin/boot
    user: discourse
    mount: /src
    setup: .github/quartermaster/setup
